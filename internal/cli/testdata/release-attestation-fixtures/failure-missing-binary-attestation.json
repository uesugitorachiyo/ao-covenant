{
  "schema_version": "covenant.release-attestation-fixture.v1",
  "name": "failure-missing-binary-attestation",
  "description": "Manifest attestation verifies, but the installed platform binary attestation is missing.",
  "expected": "fail",
  "release": {
    "version": "v0.1.0",
    "repo": "uesugitorachiyo/ao-covenant",
    "manifest": "manifest.json",
    "artifact": "ao-covenant_v0.1.0_linux_amd64",
    "target": "linux/amd64",
    "public_only": true
  },
  "checks": [
    {
      "name": "manifest",
      "artifact": "manifest.json",
      "command": "gh attestation verify manifest.json --repo uesugitorachiyo/ao-covenant",
      "required": true
    },
    {
      "name": "platform-binary",
      "artifact": "ao-covenant_v0.1.0_linux_amd64",
      "command": "gh attestation verify ao-covenant_v0.1.0_linux_amd64 --repo uesugitorachiyo/ao-covenant",
      "required": true
    }
  ],
  "failure": {
    "reason": "The platform binary has no verifying GitHub artifact attestation.",
    "failing_check": "platform-binary",
    "consumer_action": "Stop and do not install the binary until the release is replaced or corrected.",
    "sensitive_omitted": [
      "private keys",
      "credentials",
      "production evidence bundles",
      "local machine paths"
    ]
  }
}
